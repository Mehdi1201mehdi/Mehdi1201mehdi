{
  "name": "coach-perso-ia",
  "version": "0.1.0",
  "description": "Application perso de coaching sportif intelligent (PWA local-first).",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "typecheck": "tsc --noEmit -p jsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
