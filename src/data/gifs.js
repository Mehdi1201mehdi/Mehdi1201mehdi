// @ts-check
/**
 * Correspondance exercice → URL de GIF animé (généré par gifs-import.mjs via
 * GitHub Actions à partir du dataset hasaneyldrm/exercises-dataset).
 * Point d'extension : vide par défaut, réécrit par l'importeur.
 * Les GIFs sont RÉFÉRENCÉS par URL (non copiés) — attribution à la source.
 */
export const GIFS = /** @type {Record<string,string>} */ ({});
